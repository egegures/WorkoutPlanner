package model.cardio;

public class StairClimber extends Cardio {
    // REQUIRES: minutes > 0, level >= 1
    public StairClimber(int minutes, int level) {
        super(minutes, level, 0.8, "Stair climber");
    }







}
