package model.cardio;

public class IndoorBike extends Cardio {
    // REQUIRES: minutes > 0, level >= 1
    public IndoorBike(int minutes, int level) {
        super(minutes, level, 0.8, "Indoor bike");
    }






}
